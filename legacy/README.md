# legacy

这里保存早期用于验证思路的 Python、PowerShell 和批处理脚本。

这些文件只作为历史参考，不代表当前推荐实现：

- macOS 主版本请看 `LivePhotoLookerMac/`
- Windows 预览版请看 `LivePhotoViewer.WPF/`
- 新功能和缺陷修复优先进入 macOS SwiftUI 版本

如果后续某个脚本仍然有价值，可以再整理成正式工具；否则建议不要在 README 或发行包里推荐用户直接运行。
