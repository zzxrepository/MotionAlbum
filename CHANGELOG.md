# 更新日志

本项目采用语义化版本号：`主版本.次版本.修订版本`。

## 未发布

- macOS 与 Windows 新增根目录 / 当前目录分离的目录树浏览：打开大目录后建立完整媒体索引，侧边栏按层级切换子目录，图库默认只显示所选目录本层内容；“包含所选目录的子目录”只改变展示范围，不再触发整库重扫。
- 图库统计、标签、“我喜欢”、导出、安卓同步和批量回收站操作均跟随当前目录范围，顶部路径和空目录提示同步更新。
- 目录树计数改为目录分支的媒体总数，父目录不再因本层没有文件而显示 0；macOS 与 Windows 侧边栏支持拖动调整宽度并持久保存。
- 侧边栏默认宽度恢复为紧凑尺寸，整条右边界均可拖动，双击分隔线可恢复默认宽度；迁移掉开发验证期间保存的过宽值。
- 优化 macOS 与 Windows 侧边栏缩放：拖动期间不再反复写入设置，增加左右缩放光标、分隔线悬停/拖动反馈与 macOS 实时宽度提示，并根据窗口宽度动态保留图库的最小可用空间。
- macOS 与 Windows 目录树新增媒体文件节点：任何含照片或独立视频的目录都可继续展开，目录在前、文件在后；单击文件会切换到所在目录、清除遮挡它的筛选条件，并在图库中立即聚焦和定位。

- Windows 版补齐与 macOS 版一致的具体标签筛选、按时间分组开关、修改时间排序、多词元数据搜索、单张右键菜单、资源管理器定位和单张回收站操作。
- Windows 查看器新增可持久化的播放结束停留画面、原始封面恢复、单张删除后的前后张回退、缩放百分比与边界状态；图库与查看器均支持按钮、快捷键、滚轮及触控缩放。
- Windows 版新增带文件/配套视频指纹的图库索引，缓存 EXIF、GPS 和实况识别结果；文件被替换或修改时会自动使旧结果失效，避免实况误判长期残留。
- Windows 删除操作优先使用系统回收站；回收站不可用时回退到同卷隐藏的 `.MotionAlbumTrash`，并避免再次扫描该安全删除区。
- Windows 版新增侧栏状态保存、顶部短句轮播、本地 `quotes.txt`、操作进度与防连点保护；adb 同步增加确认并在完成后尝试打开微信。
- Windows 自检增加图库索引恢复及文件变化失效测试；版本更新为 `0.3.0`。

- Windows WPF 版本完成主流程重构：采用圆角块状工作区，支持侧边栏收起、整行操作、全部/实况/视频/我喜欢/标签筛选、单击当前查看、双击或 Enter 进入查看器，以及方向键/WASD 导航。
- Windows 图库缩放支持滑杆、左右缩放按钮和 Ctrl+滚轮；最大档切换为单张居中的高清展示，并按视口加载高清预览。
- Windows 版接入 Apple HEIC/JPG + MOV UUID/同名配对、华为/荣耀、小米/Google、标准 Motion Photo V1/V2（含新版 vivo）、OPPO 与通用内嵌 MP4 解析和 LibVLC 播放。
- Windows 版“我喜欢”改用空心/红色实心爱心，不再用红色卡片边框；查看历史、返回当前/上次查看、标签、导出、回收站和 adb 同步均已接入。
- 新增 Windows 自包含发布脚本与跨平台解析自检；真实样本覆盖两组 Apple Live Photo、荣耀实况提取与华为/荣耀静态误判检查。

- 优化大照片库性能：打开目录时先建立媒体列表，EXIF 元信息改为后台低并发索引；缩略图解码增加并发限制并接入磁盘缩略图缓存；macOS 使用 SQLite 图库索引，Windows 使用带文件指纹的本地 JSON 索引，减少 4000+ 照片目录重复打开时的解析开销。
- macOS 新增 SQLite 图库索引层，保存文件路径、大小、修改时间、媒体类型、配套视频、EXIF 元信息、实况识别状态和基础搜索文本，为后续分页加载与数据库搜索打基础。
- 图库新增按拍摄时间、修改时间、文件名排序，并支持自适应时间分组：小缩略图按月份聚合，大缩略图按日期展示；同时支持一键回到顶部。
- 图库新增“刚刚查看”上下文：双击查看照片后，图库卡片会显示当前查看标识；即使滚动到别处，也可以一键回到刚刚查看的位置。
- 顶部工具栏新增可轮播短句展示，默认包含公版诗词和相册主题自拟句，并支持本机 `quotes.txt` 扩展私人语句；左侧底部新增作者署名。
- 优化顶部工具栏视觉：短句轮播改为轻量文字样式，搜索框改用原生 macOS 搜索控件，作者署名弱化为低存在感底部署名。
- 打开目录会在后台索引子目录；图库默认仅显示所选目录本层，用户可按需汇总其子目录。
- 查看器支持左右方向键切换上一张 / 下一张；返回图库时会自动定位到刚刚查看的照片。
- 外接硬盘不支持 macOS 废纸篓时，删除操作会回退到同目录隐藏的 `.MotionAlbumTrash` 安全删除区，不再直接失败。
- 自检可以识别 `samples` 下一级子目录中的荣耀样本，避免样本移动后出现“未实际测试但自检通过”的假象。
- 新增类似手机相册的可缩放照片墙：支持滑杆和触控板捏合调整缩略图大小，小尺寸下隐藏冗余文字以提升大目录浏览流畅度。
- 扩展搜索范围：支持按文件名、标签、地点名称、目录名、路径、设备型号等字段搜索；搜索地点时会后台解析 GPS，并缓存解析失败结果避免重复请求。
- 支持将单张照片或精选批量移入 macOS 废纸篓，删除前确认，并清理对应精选、标签和封面帧记录。
- 支持目录中的独立 MOV/MP4/M4V 视频显示缩略图、筛选和直接播放；苹果 Live Photo 的同名 MOV 会继续作为配套视频合并到照片，不重复显示。
- 查看器支持放大、缩小、适合窗口和触控板捏合缩放。
- 优化重操作防连点保护，删除、导出、同步期间会禁用相关入口，减少快速操作导致的状态错乱。

## 0.1.0 - 2026-07-22

第一个公开预览版本。

- 支持 macOS 原生应用构建。
- 支持荣耀 / 华为实况 JPG 检测与播放。
- 支持苹果 Live Photo 常见的 `HEIC + MOV` 同名配对识别。
- 支持扫描 HEIC/HEIF 图片。
- 支持照片精选、标签、搜索、历史目录和筛选。
- 支持展示 EXIF 元信息：设备型号、拍摄时间、像素尺寸；GPS 会尽量反解析成地点名称，不直接展示裸坐标。
- 支持在主窗口内查看照片，不再使用照片弹窗。
- 支持通过胶片缩略条选择实况照片的封面 / 停留帧。
- 实况播放结束后默认回到封面图，不再停留在视频最后一帧。
- 支持导出原始照片文件。
- 导出和安卓同步会一并带上照片的同名 MOV/MP4 sidecar 文件。
- 支持通过 adb 将精选照片同步到手机 `DCIM/MotionAlbum`。
- 提供 macOS Release 打包脚本，可生成 `.zip`、SHA256 校验文件；环境支持 `hdiutil` 时也会生成 `.dmg`。
- 保留 Windows WPF 预览版源码，暂不作为正式版本发布。
