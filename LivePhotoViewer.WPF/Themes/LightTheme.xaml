<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 背景色 -->
    <SolidColorBrush x:Key="WindowBgBrush" Color="#F5F5F7"/>
    <SolidColorBrush x:Key="CardBgBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ToolbarBgBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="ViewerBgBrush" Color="#F0F0F0"/>

    <!-- 文字色 -->
    <SolidColorBrush x:Key="TextBrush" Color="#333333"/>
    <SolidColorBrush x:Key="SubTextBrush" Color="#666666"/>
    <SolidColorBrush x:Key="DimTextBrush" Color="#999999"/>

    <!-- 边框与交互 -->
    <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0"/>
    <SolidColorBrush x:Key="HoverBorderBrush" Color="#007AFF"/>
    <SolidColorBrush x:Key="ButtonHoverBgBrush" Color="#20000000"/>
    <SolidColorBrush x:Key="OverlayBgBrush" Color="#15000000"/>

    <!-- 收藏/强调 -->
    <SolidColorBrush x:Key="FavoriteBrush" Color="#FF9500"/>
    <SolidColorBrush x:Key="LiveBadgeBrush" Color="#FF2D55"/>
    <SolidColorBrush x:Key="ToggleCheckedBgBrush" Color="#30FF9500"/>

    <!-- 缩略图区域背景 -->
    <SolidColorBrush x:Key="ThumbAreaBgBrush" Color="#F5F5F7"/>
    <SolidColorBrush x:Key="ThumbPlaceholderBgBrush" Color="#F0F0F0"/>

    <!-- 通用按钮样式 -->
    <Style x:Key="IconButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#20000000"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="IconToggleButtonStyle" TargetType="ToggleButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#20000000"/>
            </Trigger>
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="{DynamicResource ToggleCheckedBgBrush}"/>
                <Setter Property="Foreground" Value="{DynamicResource FavoriteBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
